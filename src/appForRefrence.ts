// // Get Route.
// app.get("/Hello",(req,res) => res.send("Hello to Express with TypeScript!!!!!! "))

// // Router with Query
// // Url will be http://localhost:8001/user?name=ram&password=password
// app.get("/user",(req,res) => res.send(`Username is ${req.query.name},password is ${req.query.password}`))

// // Router with param

// app.get("/user/:name",(req,res) => res.send(`User name is ${req.params.name}`));

// // Router with body
// app.post("/user",(req,res) => res.send(`Username is ${req.body.name}`))
